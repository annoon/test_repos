the code task one is used to train the tree and export the trained tree this must be run before the testothergroups file can be run inside the testothergroups file you can specify the data for the other groups
